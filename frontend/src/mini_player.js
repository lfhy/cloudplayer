import { bootstrapMiniPlayerWindow } from "./windows/miniPlayer/controller.js";

// Mini-player entry keeps the floating child window boot logic isolated from the main app shell.
bootstrapMiniPlayerWindow();
